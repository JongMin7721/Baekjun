#include <stdio.h>

int main(void) // 알고리즘 다시 구상
{
    int i = 0, j = 0, a = 0, b = 0, n, m, turn = 0;
    int nm[100][100];
    scanf("%d %d", &n, &m);

    while (n != a || m != b)
    {
        turn++;
    }

    printf("%d\n", turn);
}