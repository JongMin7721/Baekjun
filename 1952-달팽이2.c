#include <stdio.h>

int main(void) // 알고리즘 다시 구상
{
    int i = 0, j = 0, a = 0, b = 0, n, m, turn = 0;
    int nm[100][100] = {0};
    scanf("%d %d", &n, &m);

    while (1)
    {
    }

    printf("%d\n", turn);
}